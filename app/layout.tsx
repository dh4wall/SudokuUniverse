import type { Metadata } from 'next';
import { ThemeProvider } from '@/components/ThemeProvider';
import Navbar from '@/components/Navbar';
import AppWrapper from '@/components/AppWrapper'; // Import the new wrapper
import './globals.css';

export const metadata: Metadata = {
  title: 'Sudoku Solver & Game',
  description: 'Play Sudoku games generated by AI or solve your own puzzles.',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="min-h-screen bg-slate-50 dark:bg-slate-950">
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {/* Use the AppWrapper to show the loader */}
          <AppWrapper>
            <Navbar />
            <main>{children}</main>
          </AppWrapper>
        </ThemeProvider>
      </body>
    </html>
  );
}